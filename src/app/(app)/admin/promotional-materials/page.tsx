// This page is deprecated and its redirect was causing server build issues.
// The route is now handled by the /admin/inventory page.
// This component returns null to prevent rendering and resolve the error.
export default function DeprecatedPromotionalMaterialsPage() {
  return null;
}
