/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí se pueden añadir futuras configuraciones de Next.js si son necesarias.
  // Por ahora, una configuración vacía pero bien formada es suficiente.
};

module.exports = nextConfig;
